#ifndef CUDASIM_CONFIG_H
#define CUDASIM_CONFIG_H

// 环境组的最大数量
#define MAX_NUM_ENV_GROUP        (4096)

// 支持最大的活动环境组数量
#define MAX_NUM_ACTIVE_ENV_GROUP (32)

// 支持最大的环境数量
#define MAX_NUM_ENV             (65536)

// 环境组中的环境数量
#define NUM_ENV_PER_GROUP       (32)

#endif