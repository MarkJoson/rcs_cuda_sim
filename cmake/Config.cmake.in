@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
find_dependency(Torch)

# 设置包含目录变量
set_and_check(RCS_INCLUDE_DIR "@PACKAGE_CMAKE_INSTALL_INCLUDEDIR@")
set_and_check(RCS_LIB_DIR "@PACKAGE_CMAKE_INSTALL_LIBDIR@")

# 导入目标
include("${CMAKE_CURRENT_LIST_DIR}/RCSStorageTargets.cmake")

check_required_components(RCS)